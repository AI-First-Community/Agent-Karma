# --- Local-only files (never commit) ---
CLAUDE.md
*.pdf
Agent Karma — *.pdf
karma-card.*
launch-posts.md
# generated at vsce:prepublish from the root CHANGELOG so it ships in the .vsix
extension/CHANGELOG.md

# --- OS / editor ---
.DS_Store
Thumbs.db
*.swp
.idea/

# --- Node / build ---
node_modules/
extension/node_modules/
extension/out/
extension/dist/
extension/*.vsix
*.tsbuildinfo

# --- Test / coverage ---
coverage/
.nyc_output/
# @vscode/test-electron downloads a full VS Code here (100s of MB)
.vscode-test/
.vscode-test.mjs
# local-only editor settings (per-machine overrides; keep out of the public repo)
.vscode/settings.json
# dev-only dashboard preview output
preview/

# --- Agent Karma local data (created at runtime) ---
agent-karma-data/

# --- Logs / env ---
*.log
.env
.env.*

# --- Private dogfooding notes (local-only) ---
DOGFOODING-NOTES.md

# Session exports (generated on demand; not part of the repo)
agent-karma-session-*.json
agent-karma-session-*.md

# --- Private marketing / launch collateral (NOT for the public repo) ---
# (the README hero images agent-karma-hero* stay tracked; these stay local)
docs/assets/flyers/
docs/assets/launch/
docs/launch/
docs/assets/agent-karma-flyers.html
docs/assets/agent-karma-gita-flyers.html
docs/assets/agent-karma-mahabharata-flyers.html
docs/assets/agent-karma-storybook.html
docs/assets/agent-karma-parallel-storybook.html
